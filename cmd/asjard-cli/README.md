## 命令

```sh
Usage: asjard-cli [OPTIONS] COMMAND

Examples:

Commands:
    new

Global Options:
    --project, -p       project
    --region, -r        region
    --avaliablezone, -z zone

```

```sh
Usage: asjard-clli [options] new COMMAND

Examples:

Commands:
    service
    protobuf

Options:

```

```sh
asjard-cli new service service_name
asjard-cli new protobuf protobuf
```
