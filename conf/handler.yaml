handler:
  loadbalance:
    # 默认负载均衡名称
    name: roundBin
    # 指定服务的的负载均衡配置
    services:
      # 服务名称
      serviceA:
        name: sessionKeep
    # 轮训负载均衡配置
    roundBin:
      xx: yy
  # 熔断器
  break: