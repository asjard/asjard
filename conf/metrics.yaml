asjard:
  metrics:
    enabled: false
    ## 支持特性
    collectors:
      - goruntime
      - apirequests
    ## 推送到pushgateway中
    pushGateway:
      ## gateway地址
      endpoint: http://127.0.0.1:9091
      ## 推送间隔
      interval: 5s
