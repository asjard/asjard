asjard:
  metrics:
    enabled: false
    ## 支持特性
    collectors:
      - goruntime
      - apirequests
    ## 推送到pushgateway中
    pushGateway:
      ## gateway地址
      endpoint: http://xxx
      ## 推送间隔
      interval: 5s
