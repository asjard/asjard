asjard:
  ## registry configuration.
  registry:
    ## if enabled, the service will register into registration center after service start
    # autoRegiste: true
    ## Delayed registration, wait for the delay time after the service starts and then register the service to the registration center
    # delayRegiste: 0s
    # heartbeat: false
    # heartbeatInterval: 5s

    ## Automatic service discovery, automatically discover services from the service center
    # autoDiscove: true
    # healthCheck: false
    # healthCheckInterval: 10s
    # failureThreshold: 1

    ## Local service discovery configuration
    localDiscover:
      ## servicename
      ## format: {protocol}://{ip}:{port}
      # helloGrpc:
      # - grpc://127.0.0.1:7010
      # - grpc://127.0.0.1:7011

    ## etcd service discovery configuration.
    etcd:
      ## etcd client name
      # client: default
      # timeout: 5s

    ## consul service disconver configuration.
    consul:
      ## consul client name
      # client: default
      # timeout: 5s
