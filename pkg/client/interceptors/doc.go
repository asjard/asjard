/*
Package interceptors provides the standard suite of client-side middleware
implementations for the Asjard framework.

These interceptors are executed in a chain for every outgoing request,
providing automated handling for:

Interceptors are registered via the `init()` function and can be enabled
or tuned through the central configuration system.
*/
package interceptors
